<Window x:Class="GroupsStudents_mvvm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:GroupsStudents_mvvm.ViewModels"
        xmlns:decanat="clr-namespace:GroupsStudents_mvvm.Models"
        xmlns:local="clr-namespace:GroupsStudents_mvvm" 
        
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel></vm:MainWindowViewModel>
    </Window.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"></ColumnDefinition>
            <ColumnDefinition Width="8*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <GroupBox Grid.Column="0" Header="Группы">
            <ListBox SelectedIndex="0" ItemsSource="{Binding Path=Groups}" DisplayMemberPath="Name" SelectedItem="{Binding Path=SelectedGroup}"></ListBox>
        </GroupBox>

        <GroupBox Background="LawnGreen" Grid.Column="1" Header="{Binding Path=SelectedGroup.Name, FallbackValue='Студенты', TargetNullValue='Группа не выбрана'}">
            <DataGrid VerticalGridLinesBrush="ForestGreen" GridLinesVisibility="Vertical" ItemsSource="{Binding Path=SelectedGroup.Students}" AutoGenerateColumns="False" IsReadOnly="False" >
            
       
                <DataGrid.Columns>
                    <DataGridTextColumn IsReadOnly="True"  Header="Фамилия" Binding="{Binding Path=Surname}"></DataGridTextColumn>
                    <DataGridTextColumn IsReadOnly="True" Header="Имя" Binding="{Binding Path=Name}"></DataGridTextColumn>
                    <DataGridTextColumn IsReadOnly="True" Header="Отчество" Binding="{Binding Path=Patronymic}"></DataGridTextColumn>
                    <DataGridTemplateColumn Header="Дата рождения">
                        <!--<DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="decanat:Student">
                                <DatePicker SelectedDate="{Binding Birthday}"></DatePicker>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>-->
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate DataType="decanat:Student">
                                <TextBlock Text="{Binding Birthday, StringFormat=dd/MM/yyyy}"></TextBlock>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>

                        <DataGridTemplateColumn.CellEditingTemplate>
                            <DataTemplate DataType="decanat:Student">
                                <DatePicker SelectedDate="{Binding Birthday}"></DatePicker>

                            </DataTemplate>
                            
                        </DataGridTemplateColumn.CellEditingTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="Рейтинг" Binding="{Binding Path=Rating}"></DataGridTextColumn>
                    <DataGridTextColumn IsReadOnly="True" Header="Группа" Binding="{Binding DataContext.SelectedGroup.Name, RelativeSource={RelativeSource AncestorType=DataGrid}}"></DataGridTextColumn>
                    <DataGridTextColumn Header="Описание" Binding="{Binding Path=Description}" Width="1*"></DataGridTextColumn>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

    </Grid>
</Window>
